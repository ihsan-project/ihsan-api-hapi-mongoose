'use strict';

const Schmervice = require('schmervice');
const Constants = require('../constants');

const internals = {
};

module.exports = class AuthenticationService extends Schmervice.Service {
    createSession(userid, type, email, transaction) {


        return {

        };
    }
};
